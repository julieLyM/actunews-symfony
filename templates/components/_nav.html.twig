<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('index') }}">ActuNews</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
                aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav me-auto">
                <a class="nav-link active" href="{{ path('default_category', {'alias':'politique'}) }}">Politique</a>
                <a class="nav-link active" href="{{ path('default_category', {'alias':'economie'}) }}">Economie</a>
                <a class="nav-link active" href="{{ path('default_category', {'alias':'santé'}) }}">Santé</a>
                <a class="nav-link active" href="{{ path('default_category', {'alias':'culture'}) }}">Culture</a>
            </div>
            <div class="navbar-nav ms-auto">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <a class="nav-link active" href="#">
                        Bonjour {{ app.user.firstname }} {{ app.user.lastname }}
                    </a>
                    {% if is_granted('ROLE_JOURNALISTE') %}
                        <a class="nav-link active" href="{{ path('post_create') }}">
                            Rediger un article
                        </a>
                    {% endif %}

                    <a class="nav-link active" href="{{ path('app_logout') }}">Deconnexion</a>
                {% else %}
                    <a class="nav-link active" href="{{ path('user_register') }}">Inscription</a>
                    <a class="nav-link active" href="{{ path('app_login') }}">Connexion</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
